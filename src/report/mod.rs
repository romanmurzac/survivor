//! The `report` module manages temporal data snapshots.
//!
//! This module defines the schema for simulation outputs, allowing the
//! application to track financial performance metrics month-by-month.
//! It provides the raw data necessary for rendering charts, tables,
//! and CSV export functionality.

/// The snapshot structure representing a single month's financial status.
pub mod report;
